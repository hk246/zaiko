# 在庫管理システム - 配布ガイド

## 実行ファイルのビルド方法

### 1. 必要なパッケージのインストール

```bash
pip install pyinstaller
```

### 2. ビルドスクリプトの実行

#### 方法A: 簡単ビルド（推奨）

```bash
python build_exe.py
```

**アイコン選択ダイアログが表示されます:**
1. デフォルトの `icon.ico` がある場合、使用するか確認されます
2. 別のアイコンファイル（.ico）を選択できます
3. アイコンなしでもビルド可能です

#### 方法B: specファイルを使用

```bash
pyinstaller inventory_app.spec
```

#### 方法C: 手動でコマンド実行

```bash
pyinstaller --onedir --windowed ^
  --name="在庫管理システム" ^
  --add-data="templates;templates" ^
  --add-data="static;static" ^
  --icon="icon.ico" ^
  --hidden-import=flask ^
  --hidden-import=flask_sqlalchemy ^
  --hidden-import=flask_wtf ^
  app.py
```

### 3. 生成されるファイル

- `dist/在庫管理システム/` - 配布用フォルダ（このフォルダ全体を配布）
  - `在庫管理システム.exe` - 実行ファイル
  - その他の必要なDLL・ライブラリファイル

## 配布方法

### 単一PC使用の場合

1. `dist/在庫管理システム/` フォルダ全体を任意の場所にコピー
   - **重要:** フォルダ内の全ファイルが必要です（.exe単体では動きません）
2. フォルダ内の `在庫管理システム.exe` をダブルクリックで起動
3. 初回起動時、データベース保存場所を選択するダイアログが表示されます
4. デフォルトでは `C:\Users\<ユーザー名>\AppData\Roaming\InventoryApp\database` に保存されます

### 複数PC での共有使用の場合

1. 各PCに `dist/在庫管理システム/` フォルダ全体をコピー
2. 共有フォルダ（ネットワークドライブなど）を用意
3. 初回起動時、データベース保存場所として共有フォルダを選択
4. 全てのPCで同じフォルダを指定すれば、同じデータベースを共有できます

### 配布時のポイント

- **ONEDIRモード**: フォルダ形式で配布
  - 利点: 起動が高速、アンチウイルスに検知されにくい
  - 注意: フォルダ内の全ファイルが必要（.exe単体では動作しません）
- フォルダをZIP圧縮して配布すると便利です
- ショートカットを作成してデスクトップに配置すると使いやすくなります

#### 注意事項
- 複数人が同時にデータベースを編集する場合、SQLiteの制限により競合が発生する可能性があります
- 本格的な複数人同時利用には、MySQLやPostgreSQLなどのサーバーDBへの移行を推奨します

## 配布先PCの要件

### 必須要件
- Windows 10 以上（Windows 7/8.1 でも動作する可能性あり）
- メモリ: 2GB以上推奨
- ディスク容量: 100MB以上

### 不要なもの
- Pythonのインストール（実行ファイルに全て含まれています）
- 追加のライブラリ（全てバンドルされています）

### 必要な場合があるもの
- Microsoft Visual C++ 再頒布可能パッケージ
  - エラーが出る場合は以下からインストール:
  - https://learn.microsoft.com/ja-jp/cpp/windows/latest-supported-vc-redist

## データの保存場所

### 設定ファイル
- **config.json**: 実行ファイルと同じフォルダ
  - 例: `dist\在庫管理システム\config.json`
  - データベースフォルダの位置が記録されます

### データベース
- **デフォルト**: `dist\在庫管理システム\instance\inventory.db`
- **カスタム**: 初回起動時に選択したフォルダ
- **バックアップ**: データベースと同じフォルダの `backups` サブフォルダ

### 設定をリセットする方法
`dist\在庫管理システム\config.json` を削除してアプリを再起動すると、再度データベースフォルダを選択できます。

### データベース
- 初回起動時に選択したフォルダ
- デフォルト: `C:\Users\<ユーザー名>\AppData\Roaming\InventoryApp\database\inventory.db`

### バックアップファイル
- データベースと同じフォルダ内の `backups` サブフォルダ

## トラブルシューティング

### 実行ファイルが起動しない
1. Windows Defender や セキュリティソフトがブロックしていないか確認
2. Visual C++ 再頒布可能パッケージをインストール
3. 管理者権限で実行してみる

### データベースが見つからない
1. 設定画面からデータベースフォルダを再設定
2. `config.json` を削除して再起動（初回設定からやり直し）

### ブラウザが自動で開かない
1. 手動でブラウザを開き、`http://127.0.0.1:5000/` にアクセス
2. デフォルトブラウザの設定を確認

### ポート5000が使用中のエラー
- 他のプログラムが5000番ポートを使用している可能性
- 解決策: app.py の最後の行を以下に変更してリビルド
  ```python
  app.run(debug=False, port=5001)  # ポート番号を変更
  ```

## ビルド時のカスタマイズ

### アイコンを変更したい場合

#### 方法1: ビルドスクリプトで選択（推奨）

1. `python build_exe.py` を実行
2. ダイアログでアイコンファイル（.ico）を選択
3. 選択したアイコンが適用されます

#### 方法2: デフォルトアイコンを配置

1. `.ico` ファイルを `icon.ico` という名前で保存
2. `python build_exe.py` を実行
3. 自動的に検出されて使用されます

#### アイコンファイルの入手方法

- **無料アイコンサイト**:
  - https://icon-icons.com/
  - https://www.iconfinder.com/
  - https://icons8.com/
- **画像をアイコンに変換**:
  - https://convertio.co/ja/png-ico/ （PNG → ICO変換）
  - https://www.favicon-generator.org/
- **サイズ**: 256x256ピクセル以上を推奨

#### 方法3: specファイルで指定

1. `inventory_app.spec` の `icon=None` を `icon='icon.ico'` に変更
2. `pyinstaller inventory_app.spec` でリビルド

### コンソールウィンドウを表示したい場合（デバッグ用）

1. `inventory_app.spec` の `console=False` を `console=True` に変更
2. リビルド

### 実行ファイルサイズを小さくしたい場合

1. UPXをインストール: https://upx.github.io/
2. `build_exe.py` に `--upx-dir=<UPXのパス>` を追加

### ONEFILEモードに変更したい場合

**現在の設定: ONEDIRモード（フォルダ形式）**

ONEFILEモード（単一の.exeファイル）に変更したい場合:

1. `build_exe.py` の `'--onedir'` を `'--onefile'` に変更
2. `inventory_app.spec` の `exclude_binaries=True` を削除し、COLLECT部分も削除
3. リビルド

**比較表:**

| 項目 | ONEDIR（現在）| ONEFILE |
|------|--------------|---------|
| ファイル数 | フォルダ内に多数 | 単一の.exe |
| 起動速度 | 高速 | やや遅い（展開処理あり） |
| サイズ | 合計100-150MB | 80-120MB |
| 配布 | フォルダごと | .exe単体 |
| アンチウイルス | 検知されにくい | 検知される可能性あり |
| 推奨 | ✓ 高速・安定 | 単一ファイル希望時 |

## セキュリティに関する注意

- 実行ファイルはウィルス対策ソフトに誤検知される可能性があります
- 企業で配布する場合は、IT部門にホワイトリスト登録を依頼してください
- デジタル署名を追加すると信頼性が向上します

## ライセンスと配布

- このアプリケーションを配布する際は、使用しているライブラリのライセンスを確認してください
- Flask, SQLAlchemy, WTForms などのライセンス条項に従ってください

## サポート

問題が発生した場合は、以下の情報を添えて報告してください：
- Windowsのバージョン
- エラーメッセージの全文
- `config.json` の内容（個人情報は除く）
